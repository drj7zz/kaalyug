# Local marketplace API (the Vercel deployment uses its built-in proxy routes).
VITE_API_URL=http://localhost:5000/api
# Optional only when your YugCoin backend permits this browser origin with CORS.
# VITE_YUGCOIN_API_URL=https://yugcoin-backend.onrender.com/api
