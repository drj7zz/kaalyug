import Marketplace from "../components/Marketplace";

/** Marketplace page — browse, filter and search published projects. */
export default function MarketplacePage() {
  return <div className="pt-[40px]"><Marketplace /></div>;
}
