import Publish from "../components/Publish";

/** Publish page — publish a project to the Kaalyug marketplace. */
export default function PublishPage() {
  return <div className="pt-[40px]"><Publish /></div>;
}
